{{ form_start(form, {'attr': {'class': 'col-6' } }) }}

{{ form_errors(form) }}
<div class="form-group">
    {{ form_label(form.content, "Votre message :") }}
    {{ form_widget(form.content) }}
    {{ form_errors(form.content) }}
</div>

{{ form_widget(form.save, {'attr': {'class': 'btn-primary'}, 'label': 'Envoyer' }) }}

{{ form_rest(form) }}

{{ form_end(form) }}